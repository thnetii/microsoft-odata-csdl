name: Node.js lint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  nodejs:
    name: Node.js
    uses: thnetii/.github/.github/workflows/nodejs-code-validation.yml@main
    with:
      checkout-parameters: >
        {
          "submodules": true
        }
      npm-install-check-enable: true
      npm-install-working-directory: .github/actions
      npm-install-arguments: >
        [
          "--workspaces",
          "--include-workspace-root"
        ]
      tsc-check-enable: true
      tsc-parameters: >
        {
          "arguments": "--noEmit\n-p\n./jsconfig.json",
          "working-directory": ".github/actions"
        }
      eslint-check-enable: true
      eslint-parameters: >
        {
          "working-directory": ".github/actions"
        }
      prettier-check-enable: true
      prettier-parameters: >
        {
          "working-directory": ".github/actions"
        }
